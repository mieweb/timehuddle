diff --git a/packages/meteor-wormhole/lib/openapi.js b/packages/meteor-wormhole/lib/openapi.js
index 1bfebed..e736512 100644
--- a/packages/meteor-wormhole/lib/openapi.js
+++ b/packages/meteor-wormhole/lib/openapi.js
@@ -9,8 +9,13 @@ import { sanitizeToolName } from './mcp-bridge';
  * @param {string} [options.restPath='/api'] - Base path for REST endpoints
  * @param {string|null} [options.apiKey=null] - Whether API key auth is enabled
  * @param {string} [options.description] - API description
+ * @param {object} [options.extraPaths] - Extra OpenAPI Path Item Objects, keyed by
+ *   path (e.g. from a plugin's `api.addOpenApiPaths()`), merged in after the
+ *   method-derived paths. Give each Path Item its own `servers` override if its
+ *   routes don't live under `restPath` (e.g. a plugin mounted at a different prefix).
  * @returns {object} OpenAPI 3.1.0 specification object
- * @throws {Error} If two method names sanitize to the same route (path key collision)
+ * @throws {Error} If two method names sanitize to the same route (path key collision),
+ *   or an extraPaths key collides with a method-derived path
  */
 export function generateOpenApiSpec(registry, options = {}) {
   const name = options.name || 'meteor-wormhole';
@@ -170,6 +175,21 @@ export function generateOpenApiSpec(registry, options = {}) {
     spec.paths[pathKey] = { post: operation };
   }
 
+  // Merge in plugin-contributed Path Item Objects (see PluginHost#addOpenApiPaths)
+  // — routes that aren't Meteor methods (e.g. binary/streaming endpoints) and so
+  // can't be derived from the method registry above.
+  if (options.extraPaths) {
+    for (const [pathKey, pathItem] of Object.entries(options.extraPaths)) {
+      if (spec.paths[pathKey]) {
+        throw new Error(
+          `Duplicate OpenAPI path key '${pathKey}' — provided by both a plugin ` +
+            'and a Meteor method. Give the plugin path a distinct key.',
+        );
+      }
+      spec.paths[pathKey] = pathItem;
+    }
+  }
+
   return spec;
 }
 
diff --git a/packages/meteor-wormhole/lib/plugins.js b/packages/meteor-wormhole/lib/plugins.js
index 3d372bf..1f17071 100644
--- a/packages/meteor-wormhole/lib/plugins.js
+++ b/packages/meteor-wormhole/lib/plugins.js
@@ -15,11 +15,18 @@ import { WebApp } from 'meteor/webapp';
  *   - options:  the resolved Wormhole init options
  *   - context:  app-provided context (auth resolvers, audit, storage, ...)
  *   - mount(path, handler): mount a connect-style handler on the web server
+ *   - addOpenApiPaths(paths): contribute hand-written OpenAPI Path Item
+ *     Objects, merged into the same spec served at the REST bridge's
+ *     /openapi.json (see RestBridge#_getExtraOpenApiPaths and
+ *     generateOpenApiSpec's `extraPaths` option). For plugins whose routes
+ *     aren't Meteor methods (e.g. a binary/streaming endpoint) and so can't
+ *     be auto-documented from the method registry.
  */
 export class PluginHost {
   constructor() {
     this._plugins = [];
     this._api = null;
+    this._openApiPaths = {};
   }
 
   /**
@@ -50,12 +57,33 @@ export class PluginHost {
       mount: (path, handler) => {
         WebApp.connectHandlers.use(path, handler);
       },
+      addOpenApiPaths: (paths) => {
+        for (const [pathKey, pathItem] of Object.entries(paths || {})) {
+          if (this._openApiPaths[pathKey]) {
+            console.warn(
+              `[Wormhole] OpenAPI path "${pathKey}" was already contributed by another plugin; overwriting.`,
+            );
+          }
+          this._openApiPaths[pathKey] = pathItem;
+        }
+      },
     };
     for (const plugin of this._plugins) {
       this._startPlugin(plugin);
     }
   }
 
+  /**
+   * OpenAPI Path Item Objects contributed by plugins via `api.addOpenApiPaths()`.
+   * Read on-demand by RestBridge when generating /openapi.json, so late
+   * plugin registrations are picked up without RestBridge needing to know
+   * about plugins at construction time.
+   * @returns {object} A shallow copy of the accumulated path-key -> Path Item Object map.
+   */
+  getOpenApiPaths() {
+    return { ...this._openApiPaths };
+  }
+
   /** Stop all plugins (reverse order) and reset host state. */
   async stopAll() {
     for (const plugin of [...this._plugins].reverse()) {
@@ -69,6 +97,7 @@ export class PluginHost {
     }
     this._plugins = [];
     this._api = null;
+    this._openApiPaths = {};
   }
 
   names() {
diff --git a/packages/meteor-wormhole/lib/rest-bridge.js b/packages/meteor-wormhole/lib/rest-bridge.js
index a18da8b..fb1bbd6 100644
--- a/packages/meteor-wormhole/lib/rest-bridge.js
+++ b/packages/meteor-wormhole/lib/rest-bridge.js
@@ -95,10 +95,14 @@ export class RestBridge {
    * @param {string} [options.version='1.0.0'] - API version
    * @param {string|null} [options.apiKey=null] - Bearer token for auth
    * @param {boolean} [options.docs] - Whether to serve Swagger UI (enabled unless explicitly `false`)
+   * @param {() => object} [options.getExtraOpenApiPaths] - Lazily-evaluated getter for
+   *   plugin-contributed OpenAPI Path Item Objects (see PluginHost#getOpenApiPaths),
+   *   merged into the generated spec on every /openapi.json request.
    */
   constructor(registry, options) {
     this._registry = registry;
     this._options = options;
+    this._getExtraOpenApiPaths = options.getExtraOpenApiPaths || (() => ({}));
     this._started = false;
     this._mounted = false;
     this._routeMap = new Map();
@@ -203,6 +207,7 @@ export class RestBridge {
           version: this._options.version,
           restPath: basePath,
           apiKey: this._options.apiKey,
+          extraPaths: this._getExtraOpenApiPaths(),
         });
         sendJson(res, 200, spec);
       } catch (err) {
diff --git a/packages/meteor-wormhole/lib/wormhole.js b/packages/meteor-wormhole/lib/wormhole.js
index 1da8442..5d0373b 100644
--- a/packages/meteor-wormhole/lib/wormhole.js
+++ b/packages/meteor-wormhole/lib/wormhole.js
@@ -89,6 +89,10 @@ class WormholeManager {
         version: this._options.version,
         apiKey: this._options.rest.apiKey,
         docs: this._options.rest.docs,
+        // Lazy: evaluated per-request in RestBridge, so plugins registered
+        // after init() (or whose start() hasn't run yet at this point) are
+        // still picked up.
+        getExtraOpenApiPaths: () => this._pluginHost.getOpenApiPaths(),
       });
       this._restBridge.start();
       console.info(
