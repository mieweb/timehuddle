/**
 * Profile feature — server-side Meteor API removed in Phase 2.
 * Data is now served by timecore (GET /v1/users/:id, PUT /v1/me/profile).
 */

export type { ProfileUpdateInput, UserProfileDoc } from './schema';
