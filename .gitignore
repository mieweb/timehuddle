node_modules
.DS_Store
*.log
packages/*/node_modules
*.tsbuildinfo
coverage
dist

# Environment files — all variants, keep only .env.example if one exists
.env*
!.env.example

# IDE
/.idea
/.vscode/*
!.vscode/extensions.json

# Build output
build/
_build
*/build-assets
*/build-chunks
.rsdoctor

# Playwright
/test-results/
/playwright-report/
.playwright-mcp/

# Local runtime secrets (never commit)
settings.json

# Local PM2 process config (machine-specific paths, never commit)
ecosystem.config.cjs

# Local Vite config override (never commit — overrides vite.config.ts in CI)
vite.config.js

# Misc local artifacts
my-submodule/*
.gitmodules
.meteor/local
