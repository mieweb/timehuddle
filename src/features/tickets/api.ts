// Tickets feature — all data now via timecore REST API (/v1/tickets).
export { type TicketDoc } from './schema';
export type {
  CreateTicketInput,
  UpdateTicketInput,
  BatchUpdateStatusInput,
  AssignTicketInput,
} from './schema';
