# Capacitor DEV build — used by `npm run dev:ios` and `npm run dev:android`.
#
# For physical device testing, this must be a URL reachable from the device:
#   - Dev server:  https://timehubbackenddev.os.mieweb.org  (if accessible)
#   - LAN IP:      http://192.168.x.x:4000                  (local backend over Wi-Fi)
#   - localhost:   http://localhost:4000                     (simulator only)
VITE_TIMECORE_URL=http://10.0.0.8:4000 
