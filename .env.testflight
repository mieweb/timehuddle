# TestFlight / staging build — frontend built in dev mode (unminified, source maps)
# connected to the staging backend.
#
# Used by: npm run testflight:ios
VITE_TIMECORE_URL=https://timecore-dev.os.mieweb.org
